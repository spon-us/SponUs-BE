# S3
cloud:
  aws:
    s3:
      bucket: ${S3_BUCKET}
      folder: images/
    stack:
      auto: false
    region:
      static: ${AWS_REGION}
    credentials:
      accessKey: ${S3_ACCESS_KEY}
      secretKey: ${S3_SECRET_KEY}

