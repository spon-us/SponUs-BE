server:
  port: 8080

spring:
  profiles:
    group:
      "test": "db, secret, s3, redis, email, firebase, security"
    default: test

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
